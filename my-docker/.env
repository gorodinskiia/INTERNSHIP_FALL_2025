# -----------------------------------------
# .env
# -----------------------------------------
# This file defines environment variables
# used by the docker-compose.yml and your app.
# Make sure not to commit secrets to GitHub!
# -----------------------------------------

# -----------------------------
# Database Configuration (PostgreSQL)
# -----------------------------

POSTGRES_USER=postgres          # Username for the PostgreSQL database
POSTGRES_PASSWORD=secret        # Password for the PostgreSQL user
POSTGRES_DB=mydatabase          # Name of the initial database to create

# These variables are read by the `db` service in docker-compose.yml

# -----------------------------
# pgAdmin (Web UI for PostgreSQL)
# -----------------------------

PGADMIN_EMAIL=admin@example.com # Email login for pgAdmin web interface
PGADMIN_PASSWORD=admin123       # Password for pgAdmin login

# Access pgAdmin at: http://localhost:8080
# Once logged in, you can add a new server with:
#   Host: db
#   Port: 5432
#   Username: same as POSTGRES_USER
#   Password: same as POSTGRES_PASSWORD

# -----------------------------
# Application Configuration
# -----------------------------

NODE_ENV=development            # Environment mode for your app (development/production)
PORT=5000                       # Port your backend app will run on inside the container

# Connection string your app will use to connect to PostgreSQL
# Format: postgres://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgres://postgres:secret@db:5432/mydatabase

# Note: "db" is the hostname of the database service as defined in docker-compose.yml
